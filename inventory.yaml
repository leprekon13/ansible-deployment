all:
  children:
    nginx_servers:
      hosts:
        192.168.1.100:        # IP-адрес сервера с Nginx
          ansible_user: diman # Пользователь для подключения по SSH
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Путь к SSH-ключу
    apache_servers:
      hosts:
        192.168.1.101:        # IP-адрес сервера с Apache
          ansible_user: diman # Пользователь для подключения по SSH
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Путь к SSH-ключу
